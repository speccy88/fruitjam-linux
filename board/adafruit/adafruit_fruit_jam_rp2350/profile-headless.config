# Headless profile overlay (remote / bench use).
# Keeps the AirLift remote stack (telnet/FTP/web); drops local display + input
# to free contiguous PSRAM on this 8 MiB no-MMU target.
# CONFIG_FRUITJAM_DVI is not set
# CONFIG_FRUITJAM_USBHOST_BRIDGE is not set
